<div class="card bg-base-100">
    <div class="flex flex-row gap-4 items-center justify-between p-6">
        <div class="card-body p-0">
            <p class="card-title text-3xl text-<%= color %>"><%= value %></p>

            <h2 class="font-semibold text-lg"><%= title %></h2>
        </div>

        <i data-lucide="<%= icon %>" class="size-16 text-stone-400"></i>
    </div>
</div>