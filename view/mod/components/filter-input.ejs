<fieldset class="fieldset p-0">
    <legend class="fieldset-legend">
        <i data-lucide="<%= icon %>" class="size-5 text-stone-400"></i>
        <%= label %>
    </legend>

    <input value="<%= value %>" name="<%= name %>" type="<%= type %>" class="input input-sm" placeholder="<%= placeholder %>" />
</fieldset>